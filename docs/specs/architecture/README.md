# Architecture

Current verified repo purpose from `README.md`:

- This repo is a Homebrew tap for jkassis public packages.
- The documented packages are `gitall` and `layout`.

The root README remains the authoritative quick-start for tap installation.
